# Инструкция по деплою GOST Formatter API на Render

## 📋 Предварительные требования

1. **GitHub аккаунт** - для хранения кода
2. **Render аккаунт** - бесплатная регистрация на [render.com](https://render.com)
3. **Anthropic API ключ** - для работы с Claude AI

---

## 🚀 Шаг 1: Создание GitHub репозитория

### 1.1 Инициализация Git репозитория

```bash
cd /Users/kachowska/Downloads/Inbox/gost_formatter

# Инициализация git
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit: GOST Formatter API"
```

### 1.2 Создание репозитория на GitHub

1. Перейдите на [github.com](https://github.com) и войдите в свой аккаунт
2. Нажмите кнопку **"+"** → **"New repository"**
3. Заполните форму:
   - **Repository name**: `gost-formatter-api`
   - **Description**: "AI-powered GOST and VAK bibliography formatter API"
   - **Visibility**: Public (или Private, если предпочитаете)
   - **НЕ** добавляйте README, .gitignore или LICENSE (они уже есть)
4. Нажмите **"Create repository"**

### 1.3 Отправка кода на GitHub

```bash
# Добавьте свой репозиторий (замените YOUR_USERNAME на ваш логин GitHub)
git remote add origin https://github.com/YOUR_USERNAME/gost-formatter-api.git

# Отправка кода
git branch -M main
git push -u origin main
```

> ⚠️ **Важно**: Убедитесь, что файл `.env` **НЕ** попал в репозиторий! Он должен быть в `.gitignore`.

---

## 🌐 Шаг 2: Деплой на Render

### 2.1 Подключение GitHub аккаунта

1. Перейдите на [dashboard.render.com](https://dashboard.render.com)
2. Если это первый раз:
   - Нажмите **"New +"** → **"Web Service"**
   - Выберите **"Connect GitHub"**
   - Авторизуйте Render для доступа к вашим репозиториям

### 2.2 Создание Web Service

1. В списке репозиториев найдите `gost-formatter-api`
2. Нажмите **"Connect"**
3. Render автоматически обнаружит файл `render.yaml` и предложит настройки

### 2.3 Конфигурация сервиса

Render автоматически использует настройки из `render.yaml`, но проверьте:

- **Name**: `gost-formatter-api`
- **Runtime**: `Python 3`
- **Build Command**: `pip install -r requirements.txt`
- **Start Command**: `uvicorn api_server:app --host 0.0.0.0 --port $PORT`
- **Plan**: `Free`

### 2.4 Установка переменных окружения

**Критически важно!** Добавьте переменную окружения:

1. Перейдите в раздел **"Environment"**
2. Нажмите **"Add Environment Variable"**
3. Добавьте:
   - **Key**: `ANTHROPIC_API_KEY`
   - **Value**: `sk-ant-api03-...` (ваш ключ из файла `.env`)

> 🔐 **Безопасность**: Никогда не коммитьте API ключи в Git! Храните их только в переменных окружения Render.

### 2.5 Запуск деплоя

1. Нажмите **"Create Web Service"**
2. Render начнёт:
   - Клонировать репозиторий
   - Устанавливать зависимости
   - Запускать приложение
3. Процесс займёт 2-5 минут

---

## ✅ Шаг 3: Проверка работоспособности

### 3.1 Получение URL

После успешного деплоя вы получите URL вида:
```
https://gost-formatter-api.onrender.com
```

### 3.2 Тестирование API

#### Проверка здоровья сервиса
```bash
curl https://gost-formatter-api.onrender.com/api/health
```

Ожидаемый ответ:
```json
{
  "status": "ok",
  "service": "GOST Formatter",
  "api_key_set": true
}
```

#### Интерактивная документация
Откройте в браузере:
```
https://gost-formatter-api.onrender.com/docs
```

#### Проверка форматирования (пример)
```bash
curl -X POST https://gost-formatter-api.onrender.com/api/format/single \
  -H "Content-Type: application/json" \
  -d '{
    "source": {
      "id": 1,
      "type": "book",
      "authors": ["Иванов И.И.", "Петров П.П."],
      "title": "Основы программирования",
      "year": 2023,
      "publisher": "Наука",
      "city": "Минск",
      "pages": "250",
      "language": "ru"
    },
    "standard": "GOST_2018"
  }'
```

---

## 🔄 Шаг 4: Автоматическое обновление

### 4.1 Настройка Auto-Deploy

Render настроен на автоматический деплой при каждом push в `main`:

```bash
# Внесите изменения в код
git add .
git commit -m "Update: добавлена новая функция"
git push origin main
```

Render автоматически:
1. Обнаружит новый коммит
2. Пересоберёт приложение
3. Задеплоит обновлённую версию

### 4.2 Отключение Auto-Deploy (опционально)

Если хотите деплоить вручную:
1. Перейдите в **Settings** → **Build & Deploy**
2. Отключите **"Auto-Deploy"**
3. Деплой будет запускаться кнопкой **"Manual Deploy"**

---

## 📊 Мониторинг и логи

### Просмотр логов
1. Перейдите в **Logs** в панели Render
2. Логи обновляются в реальном времени
3. Проверьте сообщение: `✅ API ключ найден: sk-ant-api03-...`

### Проверка статистики
```bash
curl https://gost-formatter-api.onrender.com/api/stats
```

---

## ⚠️ Важные замечания

### Бесплатный план Render

- ✅ **Бесплатно** 750 часов в месяц
- ⚠️ **Засыпание**: После 15 минут неактивности сервис засыпает
- 🐌 **Холодный старт**: Первый запрос после сна занимает ~30 секунд
- 💾 **Ограничения**: 512 MB RAM, общие CPU

### Рекомендации для production

1. **Платный план**: $7/месяц за постоянную работу
2. **Кэширование**: Добавьте Redis для ускорения
3. **Rate limiting**: Защита от злоупотреблений
4. **Мониторинг**: Подключите Sentry или LogRocket

---

## 🛠️ Устранение проблем

### Проблема: API ключ не работает
**Решение**: Проверьте переменную окружения `ANTHROPIC_API_KEY` в Render Dashboard

### Проблема: Сервис не запускается
**Решение**: 
1. Проверьте логи в Render
2. Убедитесь, что `requirements.txt` содержит все зависимости
3. Проверьте синтаксис `render.yaml`

### Проблема: 502 Bad Gateway
**Решение**: Сервис засыпает на бесплатном плане. Первый запрос разбудит его через ~30 секунд.

---

## 📞 Поддержка

- **Render документация**: [docs.render.com](https://docs.render.com)
- **FastAPI документация**: [fastapi.tiangolo.com](https://fastapi.tiangolo.com)
- **Anthropic API**: [docs.anthropic.com](https://docs.anthropic.com)

---

## ✨ Готово!

Ваш GOST Formatter API теперь доступен в интернете! 🎉

**Следующие шаги**:
- Интегрируйте API в фронтенд
- Настройте кастомный домен (опционально)
- Добавьте мониторинг и аналитику
- Рассмотрите платный план для production использования
