# 🚀 Быстрый старт: Деплой на Render

## ✅ Что уже сделано:

1. ✅ Создан `.gitignore` (исключает `.env` и другие чувствительные файлы)
2. ✅ Создан `render.yaml` (автоматическая конфигурация для Render)
3. ✅ Обновлен `requirements.txt` (добавлен `python-dotenv`)
4. ✅ Инициализирован Git репозиторий
5. ✅ Создан первый коммит

## 📝 Ваши следующие шаги:

### 1️⃣ Создайте репозиторий на GitHub

Перейдите на https://github.com/new и создайте новый репозиторий:
- **Имя**: `gost-formatter-api` (или любое другое)
- **Описание**: "AI-powered GOST and VAK bibliography formatter API"
- **Видимость**: Public или Private
- ⚠️ **НЕ СОЗДАВАЙТЕ** README, .gitignore или LICENSE (они уже есть)

### 2️⃣ Отправьте код на GitHub

Замените `YOUR_USERNAME` на ваш логин GitHub и выполните:

```bash
cd /Users/kachowska/Downloads/Inbox/gost_formatter
git remote add origin https://github.com/YOUR_USERNAME/gost-formatter-api.git
git branch -M main
git push -u origin main
```

### 3️⃣ Задеплойте на Render

1. Перейдите на https://dashboard.render.com
2. Нажмите **"New +"** → **"Web Service"**
3. Подключите GitHub (если ещё не подключен)
4. Выберите репозиторий `gost-formatter-api`
5. Render автоматически обнаружит `render.yaml` ✨
6. **ВАЖНО**: Добавьте переменную окружения:
   - Перейдите в **Environment**
   - Key: `ANTHROPIC_API_KEY`
   - Value: `your-anthropic-api-key-here` (возьмите из вашего локального файла `.env`)
7. Нажмите **"Create Web Service"**
8. Подождите 2-5 минут ⏳

### 4️⃣ Проверьте работу API

После деплоя ваш API будет доступен по адресу:
```
https://gost-formatter-api.onrender.com
```

Протестируйте:
```bash
# Проверка здоровья
curl https://gost-formatter-api.onrender.com/api/health

# Документация в браузере
https://gost-formatter-api.onrender.com/docs
```

---

## 📚 Полная инструкция

Подробная инструкция со скриншотами и примерами: **[DEPLOY_RENDER.md](DEPLOY_RENDER.md)**

---

## ⚠️ Важные заметки

- **API ключ**: Никогда не коммитьте `.env` файл! Используйте переменные окружения Render.
- **Бесплатный план**: Сервис засыпает после 15 минут неактивности (холодный старт ~30 сек).
- **Авто-деплой**: Каждый `git push` автоматически обновляет приложение на Render.

---

## 🆘 Нужна помощь?

Откройте [DEPLOY_RENDER.md](DEPLOY_RENDER.md) для решения проблем и FAQ.
